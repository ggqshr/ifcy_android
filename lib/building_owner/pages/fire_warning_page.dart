part of "building_owner_pages.dart";

class FireWarningPage extends StatelessWidget {

  final Function drawerCall;
  FireWarningPage(this.drawerCall);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
